// Stub for the Willow Dictionary compressor mentioned in constellation.compression.ts
export class WillowDictionary {
    public static compress(text: string): string {
        // Passthrough stub. A real implementation would use semantic dictionary compression.
        return text;
    }

    public static decompress(text: string): string {
        // Passthrough stub.
        return text;
    }
}
